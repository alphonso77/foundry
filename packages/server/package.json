{
  "name": "@foundry/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Foundry HTTP API — Express server wiring FolderResolver + Generator behind the contract.",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@foundry/generator": "*",
    "@foundry/shared": "*",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0"
  }
}
