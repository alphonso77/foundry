{
  "name": "@foundry/generator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Foundry generator engine — FolderResolver + Handlebars templating + config validation + zip.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@foundry/shared": "*",
    "handlebars": "^4.7.8",
    "jszip": "^3.10.1"
  }
}
