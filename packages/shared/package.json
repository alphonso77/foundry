{
  "name": "@foundry/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Foundry shared contract types — the single source of truth for blueprints, manifests, input schema, and the HTTP API DTOs.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
